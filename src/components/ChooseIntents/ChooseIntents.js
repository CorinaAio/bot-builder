import React from "react";
import IntentsList from "../IntentsList/IntentsList";

export default function ChooseIntents(props) {
  return (
    <div>
      <IntentsList {...props} />
    </div>
  );
}
